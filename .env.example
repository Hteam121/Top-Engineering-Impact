# Frontend (Vite exposes VITE_* vars to the browser)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=your-anon-key

# Ingestion scripts (server-side; never exposed to the browser)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key   # from Supabase dashboard → Settings → API
GITHUB_TOKEN=ghp_xxx                              # read-only public repo scope
